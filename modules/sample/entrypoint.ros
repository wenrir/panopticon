#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  ;;#+quicklisp(ql:quickload '() :silent t)
  )

(defpackage :ros.script.entrypoint.3906760669
  (:use :cl))
(in-package :ros.script.entrypoint.3906760669)

(defun main (&rest argv)
  (declare (ignorable argv))
  (print argv))
;;; vim: set ft=lisp lisp:
